---
const currentYear = new Date().getFullYear();

const footerLinks = {
  services: [
    'Mobile App Development',
    'Web Development',
    'Cloud Solutions',
    'DevOps & Automation',
    'Digital Strategy'
  ],
  company: [
    'About Us',
    'Our Team',
    'Careers',
    'Blog',
    'Contact'
  ]
};

const socialLinks = [
  { icon: 'fab fa-github', href: 'https://github.com/whitepuzzle' }
];
---

<footer class="bg-gray-900 text-white py-16">
  <div class="max-w-7xl mx-auto px-4 lg:px-8">
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-12 mb-12">
      <!-- Company Info -->
      <div class="space-y-6">
        <div class="flex items-center gap-2 text-xl font-bold text-primary-400">
          <i class="fas fa-puzzle-piece text-2xl"></i>
          WhitePuzzle
        </div>
        <p class="text-gray-400 leading-relaxed">
          We are an agile digital consultancy specializing in crafting simple and intuitive applications using cutting-edge open source technologies.
        </p>
        <div class="flex gap-4">
          {socialLinks.map((social) => (
            <a 
              href={social.href}
              class="w-10 h-10 bg-primary-500 rounded-lg flex items-center justify-center text-white hover:bg-primary-600 hover:-translate-y-1 transition-all duration-300"
            >
              <i class={social.icon}></i>
            </a>
          ))}
        </div>
      </div>
      
      <!-- Services -->
      <div class="space-y-6">
        <!-- <h3 class="text-lg font-bold text-primary-400">Services</h3> -->
        <ul class="space-y-3">
          <!-- {footerLinks.services.map((service) => (
            <li>
              <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300">
                {service}
              </a>
            </li>
          ))} -->
        </ul>
      </div>
      
      <!-- Company -->
      <div class="space-y-6">
        <!-- <h3 class="text-lg font-bold text-primary-400">Company</h3> -->
        <ul class="space-y-3">
          <!-- {footerLinks.company.map((link) => (
            <li>
              <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300">
                {link}
              </a>
            </li>
          ))} -->
        </ul>
      </div>
      <!-- Contact Info -->
      <div class="space-y-6">
        <h3 class="text-lg font-bold text-primary-400">Contact Info</h3>
        <div class="space-y-4 text-gray-400">
          <p class="flex items-center gap-3">
            <i class="fas fa-envelope text-primary-400"></i>
            hello@whitepuzzle.io
          </p>
          <p class="flex items-center gap-3">
            <i class="fas fa-map-marker-alt text-primary-400"></i>
            International House
            6 Canterbury Crescent,
            London, SW9 7QD, UK
          </p>
        </div>
      </div>
    </div>
    
    <!-- Bottom Bar -->
    <div class="border-t border-gray-800 pt-8 text-center text-gray-400">
      <p>&copy; {currentYear} White Puzzle Limited -  Registered in England & Wales. No: 10589601</p>
      <p>
        Made in London 🇬🇧 with &#9825;<br/>
      </p>
    </div>
  </div>
</footer>